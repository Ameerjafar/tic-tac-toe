import { useEffect } from "react";

export function GameState({ square }) {
    useEffect(() => {
        let side = true;
        for(let i = 0; i < 9; i+=3) {
        }
    }, [square]);
  return null;
}
